# توثيق نظام النسخ الاحتياطي والاسترجاع لقاعدة البيانات (SQLite Backup & Restore Guide)

يوضح هذا الدليل كيفية إدارة نظام النسخ الاحتياطي والاسترجاع الخاص بقاعدة بيانات SQLite المطورة للعمل الفعلي في نظام **Salarix**.

---

## 📂 مكان حفظ النسخ الاحتياطية (Backup Location)
يتم حفظ جميع ملفات النسخ الاحتياطي تلقائيًا في مجلد:
`./backups/`
في جذر المشروع.

يتم حفظ الملفات بتسميات متبوعة بالتوقيت الزمني الدقيق للعملية:
- النسخة الأساسية لقاعدة البيانات: `backup-YYYYMMDD-HHMMSS.db`
- نسخة ملف المعاملات المكتوبة (إذا وجدت): `backup-YYYYMMDD-HHMMSS.db-wal`
- نسخة الذاكرة المشتركة (إذا وجدت): `backup-YYYYMMDD-HHMMSS.db-shm`

---

## 🔄 1. خطوات أخذ النسخة الاحتياطية (How to Backup)

### الطريقة الأولى: النسخ الاحتياطي التلقائي (Scheduled Auto-Backups)
يقوم النظام بأخذ نسخة احتياطية بشكل دوري كل 24 ساعة بمجرد تشغيل السيرفر بدون أي تدخل خارجي.

### الطريقة الثانية: النسخ الاحتياطي اليدوي عبر الـ API (On-Demand Admin API)
يمكن لمدير النظام (Admin) تحفيز إنشاء نسخة جديدة فورًا عن طريق إرسال طلب `POST` إلى الرابط التالي:
`POST /api/database/backup`

*مثال على الاستجابة الناجحة:*
```json
{
  "success": true,
  "message": "تم أخذ النسخة الاحتياطية بنجاح",
  "data": {
    "timestamp": "2026-05-23T21:30:00.000Z",
    "backupDir": "/app/backups",
    "files": {
      "db": "backup-20260523-213000.db",
      "wal": "backup-20260523-213000.db-wal",
      "shm": "backup-20260523-213000.db-shm"
    },
    "sizeBytes": 1540096
  }
}
```

---

## 🛠️ 2. خطوات استرجاع البيانات (How to Restore Database)

في حالة حدوث خلل تقني أو فقدان للبيانات، يرجى اتباع هذه الخطوات التالية بالترتيب لاستعادة النظام:

### الخطوة 1: إيقاف تشغيل النظام (Stop the App)
أولاً، يجب إيقاف تشغيل خادم التطبيق لضمان عدم حدوث أي عمليات كتابة أثناء استبدال الملفات.

### الخطوة 2: أخذ نسخة احتياطية من الملفات الحالية من باب الأمان (Pre-restore Safety copy)
قم بنسخ ملفات قاعدة البيانات التالفة الحالية إلى مكان آخر آمن قبل البدء:
```bash
mv sqlite.db sqlite.db.corrupt
mv sqlite.db-wal sqlite.db-wal.corrupt (إن وجد)
mv sqlite.db-shm sqlite.db-shm.corrupt (إن وجد)
```

### الخطوة 3: اختيار نسخة سليمة من مجلد النسخ الاحتياطية
ابحث في مجلد `backups/` عن أحدث نسخة احتياطية سليمة تمتد بالتسمية المناسبة، على سبيل المثال:
`backups/backup-20260523-213000.db`

### الخطوة 4: نسخ الملف واستبداله بالمسار الأصلي
قم بنسخ ملف الـ `.db` الاحتياطي المختار إلى مسار قاعدة البيانات الرئيسي (المعرّف بـ `DATABASE_PATH` في ملف البيئة أو المسار الافتراضي `sqlite.db`):
```bash
cp backups/backup-20260523-213000.db sqlite.db
```

### الخطوة 5: استرجاع ملفات الدعم WAL و SHM تزامناً مع قاعدة البيانات (اختياري)
إذا كانت النسخة الاحتياطية تحتوي على ملفات `-wal` أو `-shm` مقترنة بها في مجلد backups، قم بنسخها وتسميتها بمساراتها الأصلية:
```bash
cp backups/backup-20260523-213000.db-wal sqlite.db-wal (إن وجد)
cp backups/backup-20260523-213000.db-shm sqlite.db-shm (إن وجد)
```
> **ملاحظة تقنية هامة:** نظرًا لأن النسخ الاحتياطي عبر `.backup()` في better-sqlite3 يدمج محتوى WAL مباشرة داخل ملف الـ db الأساسي بشكل متناسق ومستقر، فإن وجود ملفات `-wal` غير إلزامي للتشغيل، بل يكفي مجرد نسخ ملف `sqlite.db` الاحتياطي لتعمل قاعدة البيانات بشكل فوري ومثالي.

### الخطوة 6: بدء تشغيل خادم النظام (Restart the App)
قم بتشغيل خادم التطبيق مرة أخرى:
```bash
npm run dev
# أو للتشغيل الفعلي
npm start
```
سيقوم النظام بقراءة الملف المسترجع ومتابعة العمل بصورة طبيعية، مع تسجيل معلومات الاتصال وجاهزية البيئة الجديدة في سجل التشغيل (Console Logs).

---

## 🚨 التعامل مع حالات الطوارئ (Emergency FAQ)

#### 1. هل يؤثر النسخ الاحتياطي على أداء النظام للمستخدمين؟
**لا.** بفضل استخدام وضع **WAL Mode (Write-Ahead Logging)** مع استخدام ميزة النسخ المبرمجة غير المانعة للكتابة `sqlite.backup()`، يستمر الخادم في معالجة طلبات القراءة والكتابة بسلاسة تامة دون أي بطء أو تجميد للنظام.

#### 2. ماذا أفعل إذا ظهر خطأ "SQLite: database is locked"؟
تم ضبط وقت الانتظار التلقائي لقاعدة البيانات إلى `5000ms` (خمس ثوانٍ) لمنع تعارض العمليات المتزامنة واستمرار النظام في العمل بكفاءة عالية. إذا تكرر هذا الخطأ، يمكنك التحقق من حالة الاتصال عبر الرابط المباشر:
`GET /api/health/database`

#### 3. كيف يمكنني تغيير مسار قاعدة البيانات الفعلي للتشغيل؟
يمكنك ببساطة تحديد المتغير البيئي `DATABASE_PATH` في ملف `.env`:
```env
DATABASE_PATH=/absolute/path/to/secured/database.db
```
وسيقوم خادم النظام والنسخ الاحتياطي بالانتقال للعمل عليه دون أي إعداد يدوي إضافي.
